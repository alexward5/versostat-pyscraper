# VersoStat PyScraper

Python scripts for scraping sports data sources (fbref, FPL) and loading
them into Postgres, plus scripts to create database views.

## Setup

- Python 3.13.1+
- `pip install -r requirements.txt`

## Configuration

Create `.env.local` and set required values for database access and any
API keys used by scrapers.

